# QNM Analyser — Python dependencies
# Author: Dr. Denys Dutykh (https://www.denys-dutykh.com/)
#
# Pins are exact so that a deploy is reproducible.  Refresh with:
#   pip install --upgrade dash plotly numpy scipy kaleido gunicorn flask
#   pip freeze | grep -iE '^(dash|plotly|numpy|scipy|kaleido|gunicorn|flask)=='
dash==4.4.1
plotly==7.0.0
numpy==2.5.2
scipy==1.18.1
kaleido==1.3.0
gunicorn==26.2.0
# Imported directly by webhook.py.  Dash pulls Flask in transitively, but the
# webhook must not depend on that happening to remain true.
flask==3.1.3
